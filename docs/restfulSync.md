persistence.restfulSync.js
===================

`persystence.restfulSync.js` is a `persistence.js` plug-in that adds data
synchronization with remote servers.

Plugin in development
-----------------
